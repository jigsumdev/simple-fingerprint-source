name = "simple-fingerprint-source"
compatibility_date = "2025-04-01"
main = "src/worker.ts"

[assets]
directory = "./dist"
not_found_handling = "single-page-application"

# D1 is intentionally omitted until the production database is created.
# Once created, add:
# [[d1_databases]]
# binding = "DB"
# database_name = "precision-scanner-db"
# database_id = "<REAL_D1_DATABASE_UUID>"
# migrations_dir = "migrations"

[triggers]
crons = ["0 3 * * *"]
